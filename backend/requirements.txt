# TravelAI 旅行智脑 —— 后端依赖
# 注：本机 Anaconda 已预装下列大部分库，可直接用 Anaconda python 运行；
# 其他环境可用 pip install -r requirements.txt 安装。
fastapi>=0.109
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
pydantic-settings>=2.1
python-jose[cryptography]>=3.3
bcrypt>=4.0
email-validator>=2.0
python-multipart>=0.0.9
langgraph>=0.0.40
langchain-openai>=0.1.0
# 向量库：Milvus（默认 milvus-lite 嵌入式，无需 Docker）。
# pymilvus 提供 MilvusClient；嵌入式模式需 milvus-lite：
#   pip install "pymilvus[milvus_lite]"
# 或连接独立 Milvus 服务时仅需 pymilvus。
pymilvus>=2.5
openai>=1.0.0
